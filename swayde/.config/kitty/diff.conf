# Config for the kitty diff kitten (`kitten diff`, `git difftool`).
# Read directly by the kitten — kitty.conf is NOT loaded here, so colors
# stay at the kitten's sensible defaults until a flavor diff theme is added.
num_context_lines    5
syntax_aliases       _conf=ini zsh=bash
