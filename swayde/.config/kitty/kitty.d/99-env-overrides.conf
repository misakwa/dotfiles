# Escape hatch: any env var named KITTY_CONF_OVERRIDE_* has its value inlined
# as raw kitty config. Loaded last so overrides win.
#   e.g.  export KITTY_CONF_OVERRIDE_OPACITY='background_opacity 0.9'
envinclude KITTY_CONF_OVERRIDE_*
