[Unit]
Description=git-pkgs package-registry proxy
After=network-online.target
Wants=network-online.target

[Service]
# %h expands to $HOME natively, so no render step is needed (unlike launchd).
ExecStart=%h/bin/package-registry-proxy
Restart=always

[Install]
WantedBy=default.target
